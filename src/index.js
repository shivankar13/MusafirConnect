import React from "react";
import ReactDOM from "react-dom/client";
import MusafirConnectApp from "./App"; // Your App.js file

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MusafirConnectApp />
  </React.StrictMode>
);
